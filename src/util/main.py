from util.scheduler import Scheduler


def main() :
    print('Welcome to our Data Enginering 101 guide!')
    scheduler = Scheduler()
    scheduler.start()

if __name__ == '__main__' :
    scheduler = Scheduler()
    scheduler.start()